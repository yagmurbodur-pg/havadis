#!/bin/zsh
# Havadis chatbot'u: Külliyat + Lugat üzerinde soru-cevap (yerel, token'sız).
export PATH="$HOME/.local/bin:/opt/homebrew/bin:/usr/local/bin:$PATH"
exec /Users/yagmur/havadis/.venv/bin/python -m pipeline.sor "$@"
